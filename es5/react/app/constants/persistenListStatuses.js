const requestToListStatuses = {
    NOT: 'NOT_DATA',
    PROCESS: 'GET_DATA_IN_PROCESS',
    READY: 'DATA_READY'
};
export default requestToListStatuses;

// using import { requestToListStatuses } from '../constants';
